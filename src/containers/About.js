import React from 'react';
import BigText from '../component/BigText';

const About = () => {
    return (
        <div>
           <BigText>소개</BigText>
        </div>
    );
};

export default About;